# 贡献指南

感谢关注本项目。这是一个本地运行的求职辅助工具，涉及简历与求职数据，请在参与前完整阅读本指南。

## 红线：禁止提交任何真实个人数据

无论 issue、Pull Request、commit message、代码、注释、截图还是测试数据，**一律不得包含**：

- **真实简历**：简历全文或任何可识别求职者身份的片段（姓名、电话、邮箱、住址、身份证号、教育/任职经历等）；
- **聊天记录**：与 HR 或任何平台用户的对话内容、消息快照、联系人信息；
- **采集数据**：运行本项目采集得到的岗位数据库（`*.db`）、JSON / xlsx 结果文件、Chrome profile、Cookie 与一切登录凭证；
- **密钥凭证**：任何 API Key、Token、密码（包括日志与截图中出现的内容）。

测试与示例数据一律使用虚构内容。`docs/`、`.venv/`、`*.db` 等已在 `.gitignore` 中，**请勿使用 `git add -f` 强行添加**。如发现自己误提交了敏感数据，请立即撤回编辑并联系维护者，不要在公开渠道继续扩散。

## 开发流程

- 全项目中文注释 / 中文 UI，新代码沿用现有惯例（纯标准库 + fastapi/openai/openpyxl，4 空格缩进）。
- 每个功能必须带单测；改动 `backend/` 或 `frontend/` 后必须全量回归：

  ```bash
  .venv/bin/python -m unittest discover tests -v
  ```

- 提交信息使用 Conventional Commits；自「使用指南」里程碑完成后停用里程碑编号，新提交不得使用 `feat(Mxx)` 形式，新增测试文件以 `test_功能描述.py` 命名（详见 `AGENTS.md`）。
- 只读探针脚本放 `tests/spike_*.py`，不进入回归；单测必须在 import backend 之前设置 `BOSS_COPILOT_HOME` 临时目录（模板见 `tests/test_m1_importer.py` 头部）。
- 发送护栏（每日上限 / 随机间隔 / 去重 / 熔断）不可关闭，任何 PR 不得削弱护栏。

## 提交 PR 前自查

1. 逐行确认 diff 与截图、测试数据中不含任何真实简历、聊天记录、采集数据或凭证；
2. 全量回归全绿；
3. 说明改动内容、原因，以及是否影响护栏或状态机语义。
